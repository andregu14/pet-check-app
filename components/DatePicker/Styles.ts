import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
      width: "100%",
    },
    inputContainer: {
      position: "relative",
    },
    iconButton: {
      position: "absolute",
      right: 10,
      top: "50%",
    },
  });

export default styles